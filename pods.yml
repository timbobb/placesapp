apiVersion: v1
kind: Pod
metadata:
  name: placesap:tagVersion
  labels:
    app: placesapp
spec:
  containers:
    - name: placesapp
      image: docker.io/timbobb/placesapp:tagVersion
      ports:
        - containerPort: 8080
  restartPolicy: Always
